#include "main.h"

/**
 * _abs - point d'entré
 * @n: int code va vérifier pour ce charactère
 *
 * Return: la valeur absolue de l'intégreur
 */

int _abs(int n)
{
if (n <= -1)
{
n *= -1;
}
return (n);
}
