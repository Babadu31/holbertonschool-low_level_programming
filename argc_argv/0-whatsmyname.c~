#include <stdio.h>

/**
 * main - imprimme le nom du programme
 * @argc: nombre d'arguments dans la fonction
 * @argv: vecteur d'argument de pointeurs vers des chaînes
 * Return: toujours 0
 */

int main(int argc, char *argv[])
{
	argc = 0;
printf("%s\n, argv[argc]");
return (0);
}
