#include "main.h"

/**
 * _isupper - vérifie le caractères majuscule.
 * @c: le charactère à vérifier 
 * Return: 1 si c est en majuscule, 0 sinon
 */

int _isupper(int c)
{
return (c >= 'A' && c <= 'Z');
}
